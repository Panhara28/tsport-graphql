extend type Mutation {
  singleUpload(file: Upload!): UploadedFile!
}

type UploadedFile {
  filename: String!
  url: String!
  fileSize: String!
  mimetype: String!
  width: String!
  height: String!
}
