extend type Query {
  mediaList: MediaList
}

extend type Mutation {
  createMedia(websiteId: Int!, input: MediaInput): Int!
}

type Media {
  image_url: String
}

type MediaList {
  data: Media
  pagination: Pagination
}

input MediaInput {
  image_url: String
}
